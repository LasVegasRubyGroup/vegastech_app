h2 #VegasTech Supporters

h4 A list of the accounts who have signed into the #VegasTech Bulletin Board via Twitter.

#supporters

  ul.thumbnails

    - @users.each do |user| 
      .span2.head_shot
        .thumbnail

          = image_tag(user.twitter_profile_image_url) 
          p= link_to(user.twitter_handle ,link_from_handle(user.twitter_handle), target: :new) 
   